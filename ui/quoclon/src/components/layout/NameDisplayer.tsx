
export const NameDisplayer = (props: { name: string }) => {
    return (
        <div className="name-displayer">
            <span className="name">{props.name}</span>
        </div>
    )
}   
            